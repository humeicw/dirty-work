# 这套规则是怎么改出来的

Dirty Work 是一个私有技能的公开版。那个私有技能我从 2026 年 7 月起每天在用。
里面每一条规则，都是先出了事才加上的。

下面每一次改动都分三段写：之前的问题，做了什么调整，结果怎样。

这一页只收有文件依据的改动：一次提交记录、当天写下的决策笔记、会话记录，或者事后复盘。
"结果"里的数字都来自我自己的会话记录。没有数字的地方，我就直说没有。
这些记录只是观察：一个人、一台电脑，没有做过对照实验。

第二天（2026-07-10）我记下的一句话，到今天还是第 1 节的开头：省 token 是一回事，不弄脏主对话的上下文更重要。

先解释几个词：

- 主 Agent：和我直接对话、负责统筹的那个 AI 助手。
- 子 Agent：被主 Agent 派出去干活的另一个 AI 助手。
- 上下文：AI 在一次对话里能同时记住的内容，有上限。
- token：AI 计算文字量的单位，一个汉字大约一到两个 token。
- 工单：主 Agent 发给子 Agent 的任务说明。
- 档位：按任务难度分的四个等级，每一档配一个模型，越高越强也越贵。

下文的"第几节"指 [`SKILL.md`](skills/dirty-work/SKILL.md) 里的章节。[English](HISTORY.md)

## 1. 2026-07-10 到 07-14：二十个子 Agent，全跑在最贵的模型上

- **之前的问题。** 档位表只管住了一种派子 Agent 的方式。07-10，一次审计通过"工作流编排"（用脚本一次派出一批子 Agent 的功能）派出了 20 多个子 Agent。没有一个写明用哪个模型，于是全部沿用了主对话的模型，也就是我最贵的那个。第二天我补了一条规则。07-14 同样的事又发生了一次，这次是 12 个。原因是那条规则写在一份"用到才读"的文档里，而当时没有谁去读它。
- **做了什么调整。** 每次派活都必须写明模型。随仓库发布的每个角色文件都写死了自己的模型。第 8 节要求：不用现成角色时，必须显式写明模型和推理强度。`references/codex.md` 提醒你自己检查子 Agent 到底继承了什么。我私下还用过一个钩子（在 AI 执行动作之前自动运行的检查程序），不写模型的派活一律拦下，用到 09-02。
- **结果。** 当时：两次事故里，每一个子 Agent 都跑在最贵的模型上。现在：09-05 到 09-19 的记录里，123 次派活只有 2 次（1.6%）用了最贵的模型。

## 2. 2026-08-03 到 08-09：失败一次，不等于需要更聪明的模型

- **之前的问题。** 验收没过就无条件"升一档"。子 Agent 的回复限定在 10 行以内。评审员把活儿打回去，也被记成一次"派活失败"。08-07，两个普通任务被一路升到了最高档模型。有一档的通过率只有 44%，细看才发现，其中 3 笔"失败"其实是评审员正确地驳回了不合格的成果。另外，10 行的限制把主 Agent 真正需要的信息挤掉了。
- **做了什么调整。** 第 7 节：按任务回执计数，不按单次报错计数；环境故障不算能力不足。第 6 节：评审员判定不合格，算评审成功。第 3 节：要的是你需要的信息，回复不设固定长度。
- **结果。** 现在很少用到最高档模型（123 次里 2 次，见第 1 条）。取消行数限制之后，回复也没有变长：这批记录里，每次回复的中位数是 272 token。

## 3. 2026-08-11 到 09-02：管控机制比活儿本身还重

- **之前的问题。** 规则正文约 100 行。此外每次派活都要写一份工单文件、记一行日志，还有三个钩子，每次失败自动开一张修复单，修完再派一个全新的 Agent 重新验证。在一个项目里，一些很小的基础设施缺口（一个路径、一段 JSON、命令行输入）每次都触发整条链：开单、修复、重新验收、新 Agent 验证、再复核。没有任何停止线。我在 08-12 的记录里写的是：花掉的时间和 token"远超任务价值"。总数我没有。
- **做了什么调整。** 我先给其中一个工具把规则重写成 33 行，09-02 又把整套旧机制在两个工具上全部退役。留下来的是：第 3 节"工单不需要固定表单，也不需要单独文件"；第 7 节"不得用无限升级、无限评审掩盖设计和环境问题"。这个技能不带任何脚本。
- **结果。** 现在派一次活就是一次调用。09-03 实测过 7 张工单，平均每张约 365 token。以前每派一次，还要另外写一个文件、记一行日志、过一道钩子检查。修复单没有了，修复单引发的循环也就没有了。

## 4. 2026-09-02："这个任务看起来不大"

- **之前的问题。** 要不要派活，由主 Agent 自己判断。一个 Agent 读了规则，判断一次故障诊断是小任务，就自己动手了。结果越做越大：追日志、跨模块诊断、改代码、跑测试。一轮就用掉了 25.8 万上下文里的约 20 万。下一轮一开始就触发了压缩（对话太长时，系统自动把前面的内容缩成摘要，细节会丢），刚查到的信息随之丢失。事后这个 Agent 说自己"没有违反任何硬性规则"。它说得对，问题在于规则太软。
- **做了什么调整。** 第 2 节：凡是能独立执行的工作，默认交给子 Agent。"看起来不大"和"已经动手了"，都不是不派活的理由。第 4 节：做调查的子 Agent 是一个可以反复追问的缓存。
- **结果。** 不够。第二天的事说明了为什么。

## 5. 2026-09-03：四个小时，一次活都没派

- **之前的问题。** 我那份常驻的规则文件（每次对话都会自动加载的那份）写的是："首次考虑派活时"去读派活技能。结果一个大型整理任务跑了大约四个小时，一次都没派活。61 条命令的输出全部堆进了主对话。主对话从约 6.5 万 token 涨到约 29 万，一共 89 次模型请求。Agent 自己的解释是：它从头到尾没有"考虑"过派活，所以根本没去读那个技能。（这些数字我在 2026-09-19 对着会话记录重新核对过。）
- **做了什么调整。** 没被加载的规则不会生效。所以我把触发条件挪进了 Agent 每次都能看到的文字里：`description`（技能简介，常驻在 Agent 眼前）的第一句，加上首页要求你添加的那段常驻规则。同一天我还写了一个复盘脚本，后面多数改动都来自用它做的复盘。
- **结果。** 公开版做过一次小规模触发测试：只靠 description，12 次里触发 3 次；加上常驻段，12 次里触发 12 次；误触发 8 次里 0 次。样本小，而且测试用例和调优用例是同一批。之后 14 天里，35 个会话中有 21 个派了活。在这些会话里，88.2% 的读文件和跑命令发生在子 Agent 那边；09-03 那天这个比例是零。其中记录完整的 19 个会话里，子 Agent 一共处理了约 1,900 万 token 的材料，回到主对话的只有约 4.1 万（0.21%）。另外 14 个会话该不该派，我没有查过。

## 6. 2026-09-03 到 09-06：为了写工单，自己先把调查做了

- **之前的问题。** 规则允许写工单之前做一次"最小的只读探测"。一次复盘发现：某个诊断会话的派活本身没问题，但主对话还是读进了太多只用一次的历史材料，还把一个没核实的推断当成事实写进了工单。后来是子 Agent 证明这个推断是错的。复盘的结论是：主 Agent 把"准备好工单"理解成了"先把背景了解充分"。
- **做了什么调整。** 工单需要的是目标、授权、范围和线索，不需要答案。第 2 节："调查要派出去"；"不要为了写工单而先做调查"；不能把未知的事当成已知事实。
- **结果。** 只有一个例子，不算测量：09-06 的一个修 bug 会话里，主 Agent 自己只读了 3 个文件；24 个子 Agent 一共做了约 2,800 次工具调用，处理了约 450 万 token，回到主对话的约 6,500 token。那次复盘建议做的行为检查，我还没做。

## 7. 2026-09-15 到 09-18：两天用掉 12 亿 token

- **之前的问题。** 规则没说等子 Agent 回复的时候该干什么。档位在任务开始时选一次，之后不再变。一个整理教材资料的任务，两天用掉约 12 亿 token。事后复盘发现：63.7% 花在逐条判定上，一共 31,547 条，平均每做一个"要还是不要"的判断就花掉约 24,200 token，而且全部用的是强模型。另有 20.8% 花在什么都没产出的统筹上：主窗口 703 轮里，232 轮在等子 Agent，119 轮在反复读同一个进度文件，6 轮在列出 Agent。对比之下，用脚本完成的 17,373 页文字识别只占 1.6%。其实 09-05 的一次复盘已经指出过轮询（反复去查进度）的问题，我当时的判断是不需要加规则。
- **做了什么调整。** 浪费不在并行，在每一条的单价和空转的轮询。第 4 节："按需跟进，不要因为焦虑去查"。第 3 节：成批的逐条判定，先用小批次量出单价；长期存活的子 Agent 只保留可复用的上下文；这套技能对每一层 Agent 都适用。第 8 节："边做边重新选档"。
- **结果。** 还没有改前改后的对比数字。有一个例子：09-16（写复盘报告的当天）的一个批量整理会话，派了 10 次活，7 次给一档角色，3 次给二档角色；处理了约 140 万 token，回到主对话的约 2,700 token。也有一次没做到：那一周的一次复盘发现，一个 Agent 被连续复用了三轮，累计输入涨到 4,880 万 token。规则已经有了，但没被遵守。

## 8. 2026-09-06 到 09-18：选档，从一张价目表变成一个问题

- **之前的问题。** 最早的档位表（07-09）有六档，按模型价格排。后来变成七档，又缩成三档，09-06 变成四档并固定了检查顺序。但同一个强模型仍然同时占着二档和三档，被过度使用：09-05 到 09-19 的记录里，123 次派活有 96 次（78.0%）跑在三档模型上。两个便宜档合计只有 20.3%。
- **做了什么调整。** 档位不是任务类型，不是文件数量，也不是价格。第 8 节：选档只问一个问题（子 Agent 需要自己拿主意的部分有多大），一档配一个模型，角色固定在某一档。
- **结果。** 还没有改后的数字。78% 是基线，来自改动之前和改动前后的那段时间。

## 没写进来的

有些改动我找不到当时写下的原因（比如 `NEEDS_CONTEXT` 第一次出现是在 07-20），有些事故我没法对应到某一条具体规则。这些我宁可不写，也不猜。
